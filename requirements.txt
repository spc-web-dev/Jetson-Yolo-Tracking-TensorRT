
# Core Python packages (safe to install via pip on Jetson)
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy==1.23.5
Pillow>=9.0.0
PyYAML>=6.0
requests>=2.28.0

# Tracking dependencies (BoT-SORT/ByteTrack)
lapx>=0.5.5
scipy>=1.10

